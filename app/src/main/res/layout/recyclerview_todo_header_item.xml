<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
                name="item"
                type="com.easycodingstudio.todou.model.TodoHeader"/>
    </data>

    <TextView
            android:id="@+id/tvTodosHeader"
            style="@style/HomeTitle"
            android:textColor="@{item.getHeaderColor(context)}"
            android:text="@{item.getHeaderString(context)}"/>
</layout>