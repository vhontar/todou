<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Todou" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_mode_status_bar</item>
        <item name="colorPrimaryVariant">@color/light_mode_status_bar</item>
        <item name="colorOnPrimary">@color/light_mode</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/default_text_size</item>
        <item name="android:textColor">@color/light_mode_text_color</item>
        <item name="android:fontFamily">@font/jetbrainsmono_regular</item>
    </style>
</resources>